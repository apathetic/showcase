grow_version: ">=0.0.65"
# root: /
home: /content/pages/home.yaml

localization:
  default_locale: en

title: "Components"
meta:
  google_analytics_id: ""
  image_path: /source/images/social_image.jpg
  favicon_path: /source/images/favicon.png
  social:
    twitter: example
    facebook: example

static_dirs:
- static_dir: /dist/
  serve_at: "/assets/"
  fingerprinted: true
- static_dir: /source/images/
  serve_at: "/assets/images/"
  fingerprinted: true
- static_dir: /source/fonts/
  serve_at: "/assets/fonts/"
  fingerprinted: true

sitemap:
  enabled: true

# This is nice, but let's let npm handle all building until better JS support is available
# preprocessors:
# - kind: sass
#   name: sass
#   sass_dir: /source/sass/
#   out_dir: /dist/css/
# - kind: babel ...were that this was possible...
#   name: babel
#   js_dir: /source/js/
#   out_dir: /dist/js/


deployments:
  test:                   # Deployment name.
    destination: local    # Destination kind.
    out_dir: ~/Sites/COMPONENTS/SHOWCASE/www
    env:
      host: apathetic.github.io/showcase
      scheme: https

  wes:
    destination: git
    # repo: https://github.com/apathetic/showcase.git
    repo: git@github.com:apathetic/showcase.git
    branch: gh-pages
    env:
      host: apathetic.github.io/showcase
      scheme: https

  huge:
    destination: git
    repo: https://github.com/hugeinc/showcase.git
    branch: gh-pages
    # root_dir: /
    env:
      host: hugeinc.github.io/showcase
      scheme: https
